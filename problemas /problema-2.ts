/*
Instrucciones:
- Escribir una función que tome una cadena de palabras y devuelva la palabra más larga junto con su longitud.
- Si hay más de una palabra con la longitud máxima, devolver todas ellas.

Ejemplo:
Entrada: "El sol brilla sobre la ciudad"
Salida: Palabra más larga: "brilla", Longitud: 6

Tiempo máximo: 45 minutos
*/

function palabraMasLarga(frase: string): { palabra: string; longitud: number } | null {
    // Tu código aquí
    return null;
  }
  
  // Llamada a la función
  const resultado = palabraMasLarga("El sol brilla sobre la ciudad");
  console.log(resultado);